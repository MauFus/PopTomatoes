package popt.gui_sch;

import javax.swing.JPanel;

public class MovieLine extends JPanel {

	/**
	 * 
	 */
	private static final long serialVersionUID = 5314001806476548519L;
	
	/*private static class RectDraw extends JPanel {
       
		private static final long serialVersionUID = -3945719460841628833L;
		private static final int PREF_H = 80;
		private static final int PREF_W = 230;
		protected void paintComponent(Graphics g) {
        super.paintComponent(g);  
         g.drawRect(230,80,10,10);  
         g.setColor(Color.BLUE);  
         g.fillRect(230,80,10,10);  
        }
        public Dimension getPreferredSize() {
            return new Dimension(PREF_W, PREF_H); // appropriate constants
          }
        
        }
	
	JPanel movieLinePanel = new JPanel();
	RectDraw newrect = new RectDraw();
	MovieRectImpl m;
	
	public MovieLine() {
		newrect.setBackground(Color.BLUE);
        movieLinePanel.add(newrect);
	}

	

	    private static class ButtonHandler implements ActionListener {
	        public void actionPerformed(ActionEvent e) {
	        System.exit(0);
	        }
	        }*/
	    
	    /*public static void main(String[] args) {
	        JPanel content = new JPanel();
	        RectDraw newrect= new RectDraw();
	        newrect.setBackground(Color.RED);
	        JButton okButton= new JButton("OK");
	        JButton clearButton= new JButton("Clear");
	        ButtonHandler listener= new ButtonHandler();
	        okButton.addActionListener(listener);
	        clearButton.addActionListener(listener);
	        content.add(okButton);
	        content.add(clearButton);
	        content.add(newrect);
	        JFrame window = new JFrame("GUI Test");
	        window.setContentPane(content);
	        window.setSize(720,100);
	        window.setLocation(100,100);
	        window.setVisible(true);
	        }*/

	    }

